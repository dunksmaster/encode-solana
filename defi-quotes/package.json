{
  "name": "defi-quotes",
  "version": "1.0.0",
  "description": "Encode Solana Week 5 Exercise 9 — Pyth vs Jupiter quote comparison (quote only, no swap)",
  "private": true,
  "scripts": {
    "quote": "tsx src/quote.ts",
    "build": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@solana/web3.js": "^1.98.4"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
