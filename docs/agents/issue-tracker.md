# Issue tracker

**Tracker:** GitHub Issues on `dunksmaster/encode-solana` (via `gh`).

**PRs as a request surface:** off

**Wayfinding / tickets:** prefer GitHub Issues; for solo offline slices, local markdown under `.scratch/<feature>/issues/` is acceptable.
